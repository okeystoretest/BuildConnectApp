# Dependências
node_modules

# Build do Next
/.next
/out
next-env.d.ts
# Cache incremental do tsc: artefato de build, muda a cada compilação.
tsconfig.tsbuildinfo

# Ambiente — NUNCA commitar credenciais
.env
.env.local
.env.production
.env.*.local

# Uploads: os binários vivem no volume da VPS, não no repositório
/uploads

# Prisma
prisma/*.db
prisma/*.db-journal

# Sistema / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Pacotes de entrega
*.zip

# Ferramentas locais do Claude Code (skills). Ficam na máquina, fora do repo.
.claude/

# Anotações de trabalho — não fazem parte do código.
#
# Resumo de sessão fica NA MÁQUINA, nunca no repositório. Era uma entrada
# nominal por arquivo e virou padrão depois que um resumo novo foi commitado por
# falta de regra: o `RESUMO-CORRECAO-CRONOGRAMA.md` que estava aqui sozinho
# dependia de alguém lembrar de repetir a linha.
Instruções_BCA.md
RESUMO-*.md
